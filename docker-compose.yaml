version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - .env.local  # これを追加
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
   
